#include<iostream>
#include "Arrays.h"

int findMissingNumber(int *Array) 
{
  /* n*(n+1)/2 */  
  int missingNumber;
  int sum;
  int total;

  // Sum
  for(int i = 0; i < arr.length; i++) {
    sum = sum + arr[i];
  }

  total = arr.length*(arr.length + 1)/2

  return sum - total;
}

int main (int argc, char *argv[])
{
  
  return 0;
}
